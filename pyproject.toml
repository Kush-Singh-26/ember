[project]
name = "ember"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "lm-forge",
    "pytest>=9.0.3",
]

[tool.uv.sources]
lm-forge = { path = "../lm_forge", editable = true }
